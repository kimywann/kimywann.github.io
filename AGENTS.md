# 코드 컨벤션

이 문서는 이 저장소에서 코드를 작성하거나 수정할 때 따르는 기본 규칙이다.

## 네이밍

- 기본적으로 camelCase를 사용한다.
- 함수명은 camelCase를 사용하고, 가능한 경우 화살표 함수로 작성한다.
- Boolean 값은 가능한 경우 `is` 접두사를 사용한다.
- 상수는 UPPER_SNAKE_CASE를 사용한다. 예: `STAR_RATIO`
- 의미 없는 변수명은 사용하지 않는다.
- 범용 대문자 약어는 대문자로 표기한다. 예: `HTML`, `API`, `XML`

## 타입

- 기본적으로 `interface`를 사용하고, 필요한 경우에만 `type`을 사용한다.
- 인터페이스에는 `I`, 타입에는 `T` 접두사를 사용한다. 예: `IProps`, `TStatus`
- `any`는 사용하지 않는다.

## 구조

- 컴포넌트에 전달된 props는 전개 구문으로 사용하지 않는다.
- JSX에서는 `props.onClick`처럼 props 객체를 통해 속성에 접근한다.